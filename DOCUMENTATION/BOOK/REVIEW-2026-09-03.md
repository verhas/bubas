# Chapter 1

In the competence is not an issue, you could use a metaphore: are you safer in a town with no gun, or one where everyone has a gun and you have to trust the individuals sanity.

# Chapter 2

The last line: the dot after the end at the end of the program is optional. Also add a note that this dot is there as a tribute to Nicklaus Wirth, who created PASCAL and who was living just 7km from me in Winterthur.

"...and it is not what this book is for" it is repeated. Avoid such repetitions.

# Chapter 3

"An INTEGER used where a DECIMAL is wanted..."

we need a sample code that demonstrates it.

"A domain value cannot be turned into text at all." 

unless the embedding application provides a mean for that as part of the domain.

"Note that = is the comparison." It is not THE comparision. It is one of the comparisons, namely the equality.

"Numbers of different kinds compare fine; the" integer number is widened first. Not simply whole number, because we are talking about programming language type not the mathemactical "integer" that is also whole number.

# Chapter 4

"An asking operation must have brackets..." the explanation is totally wrong. We have a single name space. You could easily tell that the TOTAL_OF is a variable or a function. The real reason is readability.

# Chapter 5

Before section "what you cannot do" you can use the metaphor for the opaque type the letter that Rosenkrantz and guildenstern was bringing to England in the name of Hamlet. They did not know what there was, they just delivered and they were killed. It would be a nice educated metaphor, easy to remember for the people, who know Hamlet and also they may feel good that they knew.

# Chapter 8

"Every language" not. Only the programming languages. Every programming language has a list...

# Chapter 9

"When an array is the right answer"

We need sample code. Not only for when NOT TO USE an array, but also when to use one.

# Chapter 10

"It is not visible, not reviewable, and not explicable." it is not TRUE
It is visible: in the Java code.
you can review it: in the Java code

The issue is that you need to understand Java. It is possible, but not in the realm of the SME.

# Chapter 14

"ANOMALY_SCORE_OF asks a " not amodel. A model can be many thing. An LLM.

# Chapter 21

The fundamental issue with the examples is that avoiding redundancy in the sample code as a goal and understandability as a goal are contradictory.
Understandability has higher priority than redundancy.
This chapter is the first when the reader meets a sample that builds a language. And this sample does not end with a seal. It ends returning the builder.
That is a real word, complex example and not an educational one.

Make it so that even the first example is full and understandable and as simple as possible, but not simpler.

Also the explanation has to be rewrittn to reflect the changed in the sample codes you will create.

Also there is no reason at this point to explan what a logger is.
It has a whole chapter and at this point it only confuses the reader.

# Chapter 24

In the sample code

.defineStatement...

hangs in the air. It is not mentioned that this is called on the builder.

The code in the section "Before and after" is part of a larger code and it is hard to understand. Same issue that I described in chapter 21.

Also when you have a static final class Route it is a good possibility to make a note that all these classes can be inner classes, so long as long they are static.

Right before "Why this command is a command" you can add a note that this is a command" you explain that conditional evaluation of an expression is a possibility but it should be used only if there is a good reason. You can also argue in the current example setting the approver and the cost center that it would not be able to set neither of those if it does not evaluate the expression.

Later there is a sentence that says: "every function writes something." No, it does not. Why woudl they I do not know what you meant.


# Chapter 25

"Also the honest part -- ..." I do not understand what you mean.

Also start the chapter with some explanation and not sample code.

Later "send the line to a service" should be "send the line to an AI service"

"With a score..." it argues that BUBAS code is version controlled. So is Java, so it is not a good argument.

"With a verdict..." it says: "it is visible, not ... " it is visible for the engineer. It is reviewable needing an engineer, but not NOT. Also explicable. "And not version controlled in any sense" really? Java source is not version controlled?

Also: you write too many time "where the seam goes".

"gap is widest" -> "gap is THE widest" the THE is missing in many places.

# Chapter 26

Sample code uses stage.getValue() and we have no idea what stage is at this point.

generator later has to be LLM based generator.

"put the export into your build" test.

We need sample code demonstrating BubasReviewed

# Chapter 27

"The choise matters more than it looks." it is extremely LLM smelly.

The class methods are always named call and it is never documnted that this is not mandatory, you can name the method anything you like, the requirement is that there is only one public object member method.

# Chapter 28

the service has a lifecycle worth thinking about

# Chapter 29

seal once, compile once, no compile per source.

# Chapter 31

SMEs can use git, why not? Rules can have their own git repo. Git is a developer tool, but many times less technical people also use git as a repo to maintain their work and use version controll.

Authoring can mantion that we plan to create debugger and LSP as soon as there are users.


Deployment

a change IS a deployment. A rule deployment and not an application deployment. Important to make a distinction.

Some application may need rebuilding and restarting. Instead of saying it it becomes active and it does not need you can write it may not need.