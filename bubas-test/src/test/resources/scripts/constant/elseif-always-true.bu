'NO-COMPILE
' The rule reaches ELSEIF: a constant arm condition buries every arm after it.
' ERROR: always TRUE
' LINE: 10
PROGRAM P
    DECLARE n INTEGER
    n = MIRROR(0)
    IF n > 5 THEN
        n = 1
    ELSEIF "a" < "b" THEN
        n = 2
    END IF
    ASSERT "unreachable", n = 2
END.
