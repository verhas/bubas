'NO-COMPILE
' And an ELSE with nothing in it.
' ERROR: this ELSE is empty
' LINE: 8
PROGRAM P
    DECLARE n INTEGER
    n = MIRROR(1)
    IF n > 0 THEN
        n = 2
    ELSE
    END IF
    ASSERT "unreachable", n = 2
END.
