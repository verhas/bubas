PROGRAM ApproveExpense(claim Report, limit DECIMAL) RETURNS BOOLEAN
    DECLARE mealCap DECIMAL FINAL = 60.00

    mealCap = limit

    IF TOTAL_OF(claim) > mealCap THEN
        RETURN FALSE
    END IF

    APPROVE claim
    RETURN TRUE
END.
