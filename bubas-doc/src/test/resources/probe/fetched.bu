PROGRAM ApproveExpense(claimId INTEGER, limit DECIMAL) RETURNS BOOLEAN
    DECLARE claim Report
    DECLARE total DECIMAL

    claim = LOAD(claimId)
    total = TOTAL_OF(claim)

    IF total > limit THEN
        REJECT claim, "over the limit"
        RETURN FALSE
    END IF

    APPROVE claim
    RETURN TRUE
END.
