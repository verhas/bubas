/**
 * Public types shared by every BUBAS module. This module has no dependencies, so a third party
 * writing a function library depends on it alone and never on the interpreter.
 */
module bubas.api {
    exports javax0.bubas.api;
}
